DATABASE_URL="postgresql://tradepilot:tradepilot@localhost:5432/tradepilot"
PORT=4100
HOST=127.0.0.1

# Auth. Keep real secrets in local .env files.
JWT_SECRET="replace-with-a-long-random-local-secret"
JWT_EXPIRES_IN="7d"
EMAIL_SECRET_KEY="replace-with-a-long-random-local-email-secret"
# Market Data
MARKET_DATA_API_KEY=""
# EastMoney ut token. Leave empty to auto-fetch from EastMoney's page (cached 30 min).
# Set a value here only if auto-fetch fails and you need to override manually.
# EASTMONEY_UT_TOKEN=""
# Enable browser/Puppeteer mode for EastMoney API to bypass TLS fingerprinting
# USE_BROWSER=false

# AI
AI_API_KEY=""
