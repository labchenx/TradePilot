import { mockDashboard } from '@/data';

export const dashboardService = {
  getDashboard() {
    // TODO: Replace mock data with GET /portfolio/summary and related dashboard APIs.
    return mockDashboard;
  },
};

