import { mockCashFlows } from '@/data';

export const cashFlowService = {
  listCashFlows() {
    // TODO: Replace mock data with GET /cash-flows once the backend endpoint is ready.
    return mockCashFlows;
  },
};

