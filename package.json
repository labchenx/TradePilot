{
  "name": "tradepilot",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "dev": "echo Please run npm run dev:api and npm run dev:web in separate terminals.",
    "dev:api": "npm run db:up && set PORT=4100&& set DATABASE_URL=postgresql://tradepilot:tradepilot@localhost:55432/tradepilot&& pnpm --filter api start:dev",
    "dev:web": "pnpm --filter web dev",
    "api": "npm run dev:api",
    "web": "npm run dev:web",
    "build": "npm --prefix apps/api run build",
    "test": "npm --prefix apps/api run test",
    "typecheck": "npm --prefix apps/api run typecheck",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "prisma:validate": "pnpm --filter api prisma validate"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
